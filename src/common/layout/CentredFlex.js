import FlexContainer from "./FlexContainer";
import styled from "styled-components";

const CentredFlex = styled(FlexContainer)`
  justify-content: space-between;
  align-items: center;
  flex-direction: column; 
`;

export default CentredFlex;
